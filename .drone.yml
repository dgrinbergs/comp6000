kind: pipeline
type: docker
name: default

platform:
  os: linux
  arch: arm64

steps:
  - name: test backend
    image: openjdk:17-alpine
    commands:
      - cd backend
      - chmod +x gradlew
      - ./gradlew test